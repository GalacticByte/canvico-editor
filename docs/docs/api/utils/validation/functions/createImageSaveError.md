# Function: createImageSaveError()

> **createImageSaveError**(): `Error`

Creates a standardized error for when a save operation is attempted with no image loaded.

## Returns

`Error`

A new Error object with a descriptive message.
