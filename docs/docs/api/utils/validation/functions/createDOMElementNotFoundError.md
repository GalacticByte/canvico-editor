# Function: createDOMElementNotFoundError()

> **createDOMElementNotFoundError**(`selector`): `Error`

Creates a standardized error for when a required DOM element is not found.
*

## Parameters

### selector

`string`

The CSS selector that failed to find an element.

## Returns

`Error`

A new Error object with a descriptive message.
