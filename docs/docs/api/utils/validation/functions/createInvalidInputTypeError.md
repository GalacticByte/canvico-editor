# Function: createInvalidInputTypeError()

> **createInvalidInputTypeError**(`selector`): `Error`

Creates a standardized error for when the main input element is not of `type="file"`.

## Parameters

### selector

`string`

The CSS selector used to find the input element.

## Returns

`Error`

A new Error object with a descriptive message.
