# Function: createInvalidFileError()

> **createInvalidFileError**(`message`): `Error`

Creates a standardized error for file validation issues (e.g., wrong type, size).

## Parameters

### message

`string`

The specific reason why the file is invalid.

## Returns

`Error`

A new Error object with a descriptive message.
