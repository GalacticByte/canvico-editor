# Function: createCanvasContextError()

> **createCanvasContextError**(): `Error`

Creates a standardized error for when the canvas 2D rendering context cannot be obtained.

## Returns

`Error`

A new Error object with a descriptive message.
