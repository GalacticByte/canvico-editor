# Function: addClasses()

> **addClasses**(`el`, `classes`): `void`

Adds multiple CSS classes to an element at once.

## Parameters

### el

`Element`

The element to add classes to.

### classes

`string`[]

An array of class names to add.

## Returns

`void`
