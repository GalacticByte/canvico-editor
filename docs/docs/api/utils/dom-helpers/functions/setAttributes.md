# Function: setAttributes()

> **setAttributes**(`el`, `attributes`): `void`

Sets multiple attributes on an element at once.

## Parameters

### el

`Element`

The element to set attributes on.

### attributes

An object where keys are attribute names and values are the attribute values.

## Returns

`void`
